version: '3.8'

services:
  postgres:
    container_name: sales_postgres_comeia
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=sales_comeia